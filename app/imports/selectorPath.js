// This tells selectors where they are in the state tree, it's an ugly hack but
// it works for now
// NOTE: There is no parent path at the root
// import parentPath from '../selectorPath'

// Export the parent path with this path appended
export default "";
